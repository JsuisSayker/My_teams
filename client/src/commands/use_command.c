/*
** EPITECH PROJECT, 2024
** My_teams
** File description:
** use_command
*/

#include "client.h"

int use_command(char *message, char **command, client_t *client)
{
    // if (client->is_logged == false) {
    //     client_error_unauthorized();
    //     return KO;
    // } else {
    //     client->team_uuid = strdup(command[1]);
    //     client->channel_uuid = strdup(command[2]);
    //     client->thread_uuid = strdup(command[3]);
    //     client->user_input->params->team_uuid = strdup(command[1]);
    //     client->user_input->params->channel_uuid = strdup(command[2]);
    //     client->user_input->params->thread_uuid = strdup(command[3]);
    //     return OK;
    // }
    // return OK;
}
